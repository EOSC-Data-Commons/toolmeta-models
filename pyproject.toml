[project]
name = "toolmeta_models"
version = "0.2.0"
description = "Shared models for scientific tool metadata and contracts"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "SQLAlchemy>=2.0",
    "pgvector",
    "pydantic>=2.0",
]

